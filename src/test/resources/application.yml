spring:
  application:
    name: money-transfer-system
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:money_transfer_db_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: never
